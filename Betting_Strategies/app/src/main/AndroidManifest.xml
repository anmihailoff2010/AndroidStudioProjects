<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.betting_strategies">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Betting_Strategies">
        <activity
            android:name=".Logo_Activity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Betting_Strategies.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Text_Content_Activity" />
        <activity android:name=".MainActivity" android:theme="@style/Theme.Betting_Strategies.NoActionBar" />
        <activity android:name=".settings.SettingActivity" />
    </application>

</manifest>